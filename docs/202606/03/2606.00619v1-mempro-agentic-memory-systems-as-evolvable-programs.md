---
title: "MemPro: Agentic Memory Systems as Evolvable Programs"
title_zh: MemPro：作为可进化程序的智能体记忆系统
authors: "Qingshan Liu, Guoqing Wang, Wen Wu, Jingqi Huang, Xinqi Tao, Dejia Song, Jie Zhou, Liang He"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00619v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 用于长程智能体的可演化记忆构建-检索流水线
tldr: 长期任务中代理需跨上下文记忆，现有代理记忆系统遵循记忆构建-检索管道，但通常只调整记忆库而固定管道，导致与演化记忆库错位，难以应对多样故障模式。MemPro将记忆构建-检索管道整体作为可进化程序，构建版本树，进化代理迭代选择、诊断并细粒度编辑子版本。在长文本记忆评测、对话状态跟踪等四个基准上，MemPro数轮进化即超过强基线和提示进化方法，且持续改善，性能-成本比更优。这项研究首次系统级进化记忆管道，为自进化代理提供了轻量高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00619v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00619v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1660, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00619v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 772, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00619v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 796, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00619v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 773, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00619v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1645, \"height\": 1295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00619v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1563, \"height\": 656, \"label\": \"Table\"}]"
motivation: 长时间自治代理记忆系统固定管道难以克服异构任务故障和记忆库演变失配问题。
method: MemPro将整个记忆构建-检索管道视为可进化程序，通过版本树和故障引导编辑-调试实现迭代优化。
result: 在四个长上下文基准上，MemPro几次迭代即超越静态和提示进化基线，并持续改进，性能-成本比优。
conclusion: 提出首个系统级记忆管道进化框架，为自进化代理提供了有效方案，凸显全管道进化的价值。
---

## 摘要
长时域自主智能体需要记忆系统来保留历史信息、跟踪演化状态并重用有限上下文窗口之外的相关知识。现有的智能体记忆系统通常遵循记忆构建-检索（MCR）流水线，但往往在部署后主要调整记忆库，而保持周围的流水线固定不变。这种固定流水线设计难以处理异构的任务特定失败模式，并且可能与随时间在规模和结构上演化的记忆库变得不一致。为了解决这些限制，我们提出了MemPro，一个系统级演化框架，将整个MCR流水线视为可进化的程序，而不是仅调整记忆库或提示文本。MemPro维护一个可运行记忆系统实现的版本树，其中演化智能体迭代地选择有前景的版本，诊断反复出现的失败，并通过失败模式引导的编辑-调试优化来创建改进的子版本。在LongMemEval、LoCoMo、HotpotQA和NarrativeQA上的实验表明，MemPro在几次迭代内就持续优于强大的静态和提示级演化基线，并随着演化继续改进，实现了良好的性能-成本权衡。代码见https://github.com/wanghai673/MemPro。

## Abstract
Long-horizon autonomous agents require memory systems to retain historical information, track evolving states, and reuse relevant knowledge beyond finite context windows. Existing agentic memory systems typically follow a memory construction-retrieval (MCR) pipeline, but often adapt mainly the memory bank while keeping the surrounding pipeline fixed after deployment. This fixed-pipeline design struggles to handle heterogeneous task-specific failure modes and can become misaligned with memory banks that evolve in scale and structure over time. To address these limitations, we propose MemPro, a system-level evolution framework that treats the entire MCR pipeline as an evolvable program rather than adapting only the memory bank or prompt text. MemPro maintains a version tree of runnable memory-system implementations, where an Evolving Agent iteratively selects promising versions, diagnoses recurring failures, and creates improved child versions through failure-mode-guided edit-debug refinement. Experiments on LongMemEval, LoCoMo, HotpotQA, and NarrativeQA show that MemPro consistently outperforms strong static and prompt-level evolving baselines within a few iterations, continues to improve with evolution, and achieves a favorable performance-cost trade-off. Code is available at https://github.com/wanghai673/MemPro.

---

## 论文详细总结（自动生成）

# MemPro: Agentic Memory Systems as Evolvable Programs 论文深度分析总结

## 1. 研究动机与核心问题
- **核心问题**：长时域自主智能体需要记忆系统来保留和重用历史信息，但现有多数智能体记忆系统在部署后仅自适应更新**记忆库**，而保持**记忆构建-检索（MCR）流水线**固定不变。
- **固定流水线的两大局限**：
  - **任务异构性**：不同长期记忆任务具有不同失败模式，需要不同的记忆使用策略，而固定流水线无法适应。
  - **记忆-流水线失配**：记忆库在规模和结构上随时间演化，固定流水线可能不再匹配当前记忆的组织和使用方式。
- **现有方法的不足**：提示级优化方法（如GEPA、MetaMem）仅能修改文本提示，无法改变可执行的流水线逻辑，因此无法根本解决上述问题。
- **整体含义**：论文主张对智能体记忆系统进行**系统级自演化**，即将整个MCR流水线视为一个**可进化的程序**，同时优化其中的提示与可执行代码。

## 2. 方法论
### 2.1 核心思想：MemPro框架
- 将**MCR流水线**（包含记忆构建、检索、整合、反思与答案生成等阶段）视为可演化程序。
- 通过维护**版本树**实现迭代演化，由**演化智能体（Evolving Agent）** 驱动，采用**失败模式引导的编辑-调试精炼**策略。

### 2.2 关键技术细节
#### MCR流水线
- **记忆库构建**：记忆代理接收结构化片段与旧记忆，生成更新增量并合并。
- **记忆检索与使用**：研究代理逐步执行检索、整合、反思，直至信息充足后生成答案。
  - 检索：根据问题与当前研究状态确定需要什么信息。
  - 整合：将检索到的信息与当前状态合并更新。
  - 反思：判断是否需要继续检索。

#### 版本树与演化流程
- **版本树 T = (V, E)**：每个节点 v 存储一个可运行的MCR流水线版本 F_v 及评估日志 L_v（整体得分、类别得分、父节点、总体评估）。
- **演化循环（T代迭代）**：
  1. **选择**：演化智能体根据所有日志选择最有前景的节点 v*_ℓ，并输出诊断分析 R_ℓ。
  2. **扩展（内迭代编辑-调试）**：
     - 从选中版本出发，演化智能体可在以下动作中选择：EDIT（修改流水线）、DEBUG（在训练集子集上执行并分析轨迹，更新诊断）、TERMINATE（结束扩展，产出新版本）。
     - 通过这种途径避免全量训练集重评估，提高效率。
  3. **评估**：新版本在全量训练集上执行，收集轨迹，由演化智能体总结生成评估日志，并作为新节点加入版本树。

#### 失败模式引导
- 评估日志中记录整体失败模式及改进方向。
- 扩展阶段利用诊断分析和具体失败案例轨迹进行针对性编辑和调试，保证演化方向聚焦于通用性改进而非单例过拟合。

### 2.3 公式化过程
- 选择：`(v*_ℓ, R_ℓ) = Aevo(Isel, {Lv})`
- 扩展中动作选择：`a_j = Aevo(Iexp, F_uℓ^(j), R_ℓ^(j))`
- 编辑：`F_uℓ^(j+1) = Aevo(Iexp, F_uℓ^(j), R_ℓ^(j))`
- 调试：`R_ℓ^(j+1) = Aevo(Iexp, R_ℓ^(j), x_j^diag, τ_j)`
- 评估：`L_uℓ = Aevo(Ieval, F_uℓ, Γ_uℓ)`

## 3. 实验设计
### 3.1 数据集与场景
- **长期记忆基准**：LongMemEval（多会话记忆）、LoCoMo（长期对话记忆）。
- **长上下文QA基准**：HotpotQA（多跳问答，56K/224K/448K上下文）、NarrativeQA（全文档叙事理解）。
- **训练/测试划分**：LongMemEval 和 LoCoMo 随机抽取10%（题目不重复）用于演化，其余测试；HotpotQA 按上下文长度约23%题目用于训练；NarrativeQA 约12%用于训练。

### 3.2 评估指标
- 记忆基准：LLM-as-a-Judge，使用 GPT-4o-mini 作为评判模型，准确率评分。
- QA基准：词级F1分数。

### 3.3 对比方法
- **直接上下文**：Full Text（全历史）、Naive RAG。
- **静态智能体记忆系统**：LangMem、Mem0、A-MEM、MemoryOS、LightMem、SimpleMem、GAM。
- **提示级演化方法**：GEPA、MetaMem。
- **MemPro变体**：MemPro-5/10/15（不同演化迭代数）。

## 4. 资源与算力
- 论文明确提及的算力使用：
  - 任务推理：主要使用 `gpt-4o-mini` 和 `Qwen3-30B-A3B-Instruct-2507`，以及部分实验使用 `Qwen2.5-14B`。
  - 演化智能体：基于 `gpt-5.4-medium` 并利用 OpenAI Codex 工具。
  - Qwen3-30B-A3B 部署于 **8块H800 GPU**，使用 **bfloat16** 精度，SGLang引擎。
- **训练时长**：未明确给出总时长，仅提及最大演化迭代数设为15，内循环最大次数20。
- **成本分析**：提供了token成本与准确率的权衡图，但未具体说明演化阶段总token消耗。

## 5. 实验数量与充分性
### 5.1 实验数量
- **4个数据集**，每个数据集多个子类别或上下文长度设置（LongMemEval 6类，LoCoMo 4类，HotpotQA 3种上下文，NarrativeQA）。
- **两组骨干模型**下评估（gpt-4o-mini 和 Qwen3-30B-A3B，部分实验用Qwen2.5-14B）。
- **对比方法**约 12 个。
- **消融实验**：4项关键设计消融（去除代码编辑、版本树、外迭代、内迭代膨胀）及检索工具消融（去除BM25、嵌入、页面ID）。
- 演化迭代趋势：展示从1代到15代的性能提升。
- 效率分析：准确率 vs. token 成本图。
- 所有结果报告**三次运行的平均值**，保证稳定性。

### 5.2 充分性与公平性
- 训练/测试题目完全独立（question-disjoint split），防止泄漏。
- 基线方法均使用相同的演化子集和预算，并在相同评估协议下比较。
- 部分基线结果直接采用原论文公开数据，部分在自己的设置下复现，确保对比的公平性。
- 消融实验全面，覆盖主要创新点。

## 6. 主要结论与发现
- MemPro 在**5次迭代内**即超越所有静态和提示级演化基线，达到SOTA。
- 随着演化进行，性能**持续提升**，显示系统级演化的高天花板。
- 系统级编辑（修改可执行代码）相比仅提示编辑**贡献显著**，验证了核心主张。
- 版本树优于链式演化，可避免单一路径退化，允许从强历史版本分支。
- 多轮外迭代和迭代内扩展都是必要组件，分别支撑持续改进和编辑质量。
- 检索模块中BM25、嵌入和页面ID检索**互补**，BM25贡献最大。
- 在性能-成本权衡上，MemPro以合理的token消耗获得最高精度。

## 7. 优点（亮点）
- **概念新颖**：首次将智能体记忆流水线整体视为可演化程序，突破固定流水线局限。
- **技术扎实**：版本树与失败模式引导的编辑-调试机制设计巧妙，贯穿系统级演化思想。
- **实验全面**：覆盖记忆基准和长上下文QA，多骨干模型、多基线对比，消融和效率分析充分。
- **现实意义**：强调离线演化成本可被后续复用摊薄，成本可控，具备应用潜力。
- **可复现性**：提供代码仓库，报告运行均值，骨干模型和设置描述详细。

## 8. 不足与局限
- **演化算力成本未明**：虽分析了推理token成本，但离线演化阶段的精确开销（GPU时、总token）未详细说明。
- **骨干相关性**：模型迁移实验仅更换推理模型，未对演化智能体进行不同配置的探索；性能提升高度依赖强大的演化模型（gpt-5.4-medium）。
- **类别不均衡问题**：在LongMemEval的“单助手”类别上存在背骨相关的偏弱表现，演化信号稀疏，可能需类别感知优化。
- **演化拓扑未探索**：仅采用树结构（单亲本衍生），未考虑图结构或合并操作，可能限制多样性。
- **任务类型局限**：实验集中在QA和记忆对话场景，真实世界开放性交互的覆盖度未知。
- **安全与隐私部署**：仅简要提及隐私与沙盒审查，未设计内置安全机制。

（完）
