---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: Anything2Skill：将外部知识编译为可供智能体复用的技能
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v2"
tags: ["query:agent-memory"]
score: 9.0
evidence: 将外部知识编译为智能体的可复用、可检索、可执行技能
tldr: "现有检索增强生成（RAG）仅检索碎片化陈述性证据，迫使代理反复从文档推断任务过程。本研究提出Anything2Skill框架，通过技能树引导的分解与重组，将外部异构知识编译为可重用、可检索、可执行的技能契约，并管理于持久化SkillBank。在qsv和GitHub-CLI任务中，结合RAG的Anything2Skill成功率达98.85%和94.10%，显著超越纯RAG方案，表明将隐式程序知识编译为显式技能是延伸检索增强代理能力的有效途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有RAG仅提供零散陈述性知识，代理需重复推理任务流程，缺乏可复用的程序性指导。
method: 提出Anything2Skill框架，基于技能树先验从知识记录中提取技能，构建结构化契约并存入SkillBank，供推理时检索复用。
result: "在qsv和GitHub-CLI上，结合RAG的Anything2Skill获得98.85%和94.10%成功率，大幅优于仅用RAG的代理。"
conclusion: 将外部知识中的隐式程序技能显式编译为可重用技能，能有效提升检索增强代理的任务执行能力。
---

## 摘要
检索增强生成（RAG）使智能体能够在推理时访问外部知识，但它主要检索碎片化的陈述性证据，智能体仍需反复从段落、手册、示例、日志或轨迹中推断任务流程。这引出了一个根本问题：从外部知识库中提取的技能能否被安装到智能体中，使其快速逼近领域专长？本文提出了 Anything2Skill，一个由分类法引导的框架，将异构的外部知识编译为可供智能体复用、检索和执行的技能。给定一个知识记录语料库，\textsc{Anything2Skill} 首先将每条记录分解为证据窗口，并在技能树先验下执行规划-扩展式技能提取。提取的候选技能随后被转换为结构化的技能契约，其中指定了调用条件、禁忌症、动作移动、工作流步骤、约束、输出规范、支持证据和置信度分数。为构建可部署的程序性记忆，Anything2Skill 通过分类感知编译、注册表级协调、生命周期追踪、版本化更新和可见的技能树投影，在持久的 SkillBank 中管理提取的技能。推理时，智能体既从原始知识库中检索任务相关的段落，也从 SkillBank 中检索相关的程序性技能，从而使 RAG 提供陈述性证据，而编译的技能提供可复用的程序指导。在 qsv 和 GitHub-CLI 上的实验表明，Anything2Skill 结合 RAG 分别达到了 98.85% 和 94.10% 的成功率，大幅优于仅使用 RAG 的智能体。这些结果表明，将潜在的程序性知识编译为显式技能，是将检索增强型智能体从知识访问扩展到能力复用的有效途径。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义  
- **研究动机**：现有检索增强生成（RAG）在推理时为智能体提供碎片化的陈述性证据（如段落、手册、示例），但智能体仍需反复从这些碎片中重新构建执行任务的程序性知识（“如何做”），无法固化、复用已习得的技能。  
- **核心问题**：能否将外部知识库中隐含的程序性知识显式提取、结构化存储，使智能体在遇到相似任务时直接调用可复用的“技能”，从而减少推理负担、快速逼近领域专家水平？  
- **整体含义**：论文提出一种从异构知识记录编译可重用技能的方法，将 RAG 从单纯的“知识访问”延伸至“能力复用”，填补了当前 RAG 系统在程序性知识管理上的空白。

### 2. 论文提出的方法论  
**核心思想**：以技能分类法（Skill Taxonomy）为结构先验，自动从文档、手册、对话、日志等异构记录中提取可执行的“技能契约”，再通过持久的 SkillBank 进行统一管理，最终在推理时同时检索陈述性证据和程序性技能。  

**关键技术细节与流程**：  
- **技能分类法（T）**：定义稳定的技能资产类型（宏协议、会话技能、微技能、安全规则、知识参考），提供层级、父‑子关系和领域归属，约束提取产物为程序性而非自由摘要。  
- **证据窗口分解**：每条记录 D 被切割为多个窗口，每个窗口包含文本片段、标题上下文、源定位等，确保每个技能均可溯源。  
- **规划‑扩展式提取**：先由 LLM 在窗口内规划出候选技能的轻量目标、资产类型、调用条件等；再逐一扩展为完整的技能草案。若窗口证据不足则返回空，防止幻觉。仅保留至少包含一个程序成分（步骤、动作、约束等）且可映射至分类法的候选。  
- **结构化技能契约**：每个技能 s 由名称、描述、归一化资产类型、分类节点、调用条件 σ⁺、禁忌条件 σ⁻、动作移动 m、工作流步骤 p、约束 c、输出合约 o、支持证据集 E、置信度 ρ 等组成。  
- **SkillBank 管理**：  
  - 文档级编译：按分类感知标识键（资产类型、粒度、节点、名称、输出等）分组草案，用编译器生成规范技能，保证不越出证据范围。  
  - 注册表级协调：对入库技能与已有技能进行混合检索（稠密+稀疏+分类兼容性），由管理模型预测生命周期动作（CREATE、STRENGTHEN、REVISE、MERGE、SPLIT、UNCHANGED、DISCARD），并受分类约束（如不同资产类型不能合并）。  
  - 版本化与可见树投影：记录每次更新的版本，将活跃技能按域‑族‑层级组织为树结构，并进行同级去重合并，重写层次边。  
- **技能增强检索**：推理时，智能体同时从原始知识库检索相关片段 C_q 和从 SkillBank 检索相关技能 S_q，两者共同指导任务解答。

### 3. 实验设计  
- **数据集与场景**：  
  - `qsv`：基于 qsv CSV 命令行工具的官方文档，评估智能体选择命令、组合选项、完成表格数据变换的能力。  
  - `GitHub-CLI`：基于 GitHub CLI 操作指南和 gh 命令文档，覆盖 110 篇源文档，总计编译出 179 个技能，评估仓库管理操作（Issue、PR、Release 等）。  
- **基准（Benchmark）**：成功完成命令行任务的百分比（Success Rate）。  
- **对比方法**：  
  - Base Agent：无任何外部上下文。  
  - RAG：仅检索原始知识库中的 top‑3 文档片段。  
  - ANYTHING2SKILL：仅从 SkillBank 检索编译技能。  
  - ANYTHING2SKILL + RAG：同时提供陈述性证据和程序性技能。

### 4. 资源与算力  
- 文中**未明确说明**所使用的 GPU 型号、数量及训练/编译时长。仅提及所有配置使用相同的基础模型 “gpt5.4”，未给出参数规模或具体硬件环境。因此算力需求无法量化。

### 5. 实验数量与充分性  
- 共在 **2 个数据集 × 4 种配置** 下进行实验，即 8 项主要结果，报告主表成功率。  
- 实验聚焦于有无技能、有无 RAG 的对比，设计简洁直接，能清晰反映技能编译的增益及其与 RAG 的互补性。  
- **充分性与客观性**：  
  - 实验场景虽然有限（两个 CLI 工具），但二者性质不同（qsv 侧重命令文档，GitHub-CLI 更复杂），能初步验证方法的通用性。  
  - 未进行消融实验（如去除分类引导、不同检索数量、不同基础模型影响等），也缺少在不同领域（如非 CLI 任务）上的评估，实验丰富度一般。  
  - 对比方法公平，所有配置均使用相同基础模型和任务协议。

### 6. 论文的主要结论与发现  
- 仅使用编译技能（ANYTHING2SKILL）即可显著提升基础智能体性能（qsv: 81.60%→91.95%，GitHub-CLI: 64.70%→82.30%），说明 SkillBank 提供了可独立使用的程序性知识。  
- 编译技能与 RAG 具有强互补性：RAG 提供任务专项事实，技能提供可复用的执行流程，二者结合在 qsv (98.85%) 和 GitHub-CLI (94.10%) 达到最优，较 Base Agent 分别提升 17.25% 和 29.40%。  
- 复杂工作流（如 GitHub-CLI）对程序性技能的依赖更大，编译技能带来的增益也更为显著。  
- 核心结论：将外部知识中的隐式程序知识编译为显式、可复用技能，是实现任务能力积累与复用的有效方向。

### 7. 优点（亮点）  
- **结构化技能契约**：定义清晰的技能字段（调用条件、禁忌、步骤、证据等），既可解释又可溯源。  
- **分类法引导的完整生命周期管理**：从提取、编译、协调到版本化、去重和树投影，形成一套完整的技能工程化框架。  
- **与 RAG 的互补设计**：不取代 RAG，而是以可插拔方式增强，使智能体同时具备陈述性事实和程序性经验。  
- **在复杂 CLI 任务上效果显著**，展示了从文档到技能的实用转化能力。

### 8. 不足与局限  
- **实验覆盖有限**：仅在两个 CLI 基准上验证，缺少更广泛领域（如 Web 操作、机器人、复杂工具调用）的评估，泛化性有待证明。  
- **对分类法的依赖**：技能类型、层级和兼容规则需预先定义，人工设计成本高，可能限制对新领域的快速迁移。  
- **缺乏与技能学习方法对比**：未与基于交互或代码生成的技能库方法（如 Voyager、DreamCoder 等）直接比较。  
- **算力与效率未报告**：未说明编译 SkillBank 或管理流程的资源消耗与耗时，对于实际部署的可行性评估不足。  
- **技能冲突与冗余处理有限**：虽定义了合并规则，但复杂情形下（如互为矛盾的技能）未见详细处理机制。

（完）
